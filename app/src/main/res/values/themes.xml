<resources>
    <!-- Base application theme. We use the system's built-in Material NoActionBar theme.
         Since Jetpack Compose renders the UI entirely in Kotlin, the XML theme is only 
         used for the basic window backdrop and requires no external dependency linking. -->
    <style name="Theme.Checkealo" parent="android:Theme.Material.NoActionBar">
        <!-- Optional system UI adjustments can be made here -->
    </style>
</resources>
